version: 2.1

orbs:
  fastlane: 2.214.0

jobs:
  build:
    macos:
      xcode: "12.5.0"
    steps:
      - checkout
      - run:
          name: Install Fastlane
          command: bundle install
      - run:
          name: Fastlane Test
          command: bundle exec fastlane test

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
